import { render, screen } from '@testing-library/react';
import { {{pascalCase name}}Page } from './{{pascalCase name}}Page';

jest.mock('react-i18next', () => ({
  useTranslation: () => ({
    t: (key: string) => key,
  }),
}));

describe('<{{pascalCase name}}Page />', () => {
  it('renders the page with props correctly', () => {
    render(<{{pascalCase name}}Page requiredProp="Exemplo de propriedade obrigatória" />);

    expect(screen.getByText('{{camelCase name}}Page.title')).toBeInTheDocument();
    expect(screen.getByText('{{camelCase name}}Page.description')).toBeInTheDocument();
    expect(screen.getByText('Exemplo de propriedade obrigatória')).toBeInTheDocument();
  });
});
